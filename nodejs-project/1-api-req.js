const https = require('https');

https.get('https://geocode.xyz/paris', (res) => {
  const réponse = '';
  // TODO: récupérer la réponse de l'API au format JSON, puis l'afficher
  console.log(réponse);
});
