const https = require('https');

https.get('https://jsonplaceholder.typicode.com/photos/1', (res) => {
  const réponse = '';
  // TODO: récupérer la réponse de l'API au format JSON, puis l'afficher
  console.log(réponse);
});
